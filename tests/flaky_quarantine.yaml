# Flaky Test Quarantine List
# Format: nodeid: reason
# Tests listed here will be marked as xfail in pytest runs
# Automatically managed by flaky detection and rehab workflows

# Example entries (remove when real flaky tests are detected):
# tests/example/test_flaky.py::TestClass::test_sometimes_fails: "intermittent on windows"
# tests/service/test_api.py::test_racey_case: "race condition under load"